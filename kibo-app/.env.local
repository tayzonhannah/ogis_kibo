# Local only. Git-ignored.
# Both values below are browser-safe by design.

NEXT_PUBLIC_SUPABASE_URL=https://zlhmoatlyasuizruyzfp.supabase.co
# Publishable key (sb_publishable_...). Replaced the legacy anon JWT after the
# legacy keys were disabled. Browser-safe by design; every RLS policy assumes
# an attacker has it.
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_zYkhTeUSt4GamEnbntj2fw_bE0vfBPS

# Not needed until Phase 5. Leave commented out.
# SUPABASE_SERVICE_ROLE_KEY=
# GEMINI_API_KEY=
# CRON_SECRET=
